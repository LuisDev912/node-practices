# 04-directory-listing

This practice is a **Command Line Interface (CLI)**.

This script reads a directory given from `process.argv[2]` and then it returns its content.

## Usage

``` bash
node index.js